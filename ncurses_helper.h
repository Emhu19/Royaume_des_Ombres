#ifndef NCURSES_HELPER_H
#define NCURSES_HELPER_H

#include <ncurses.h>

void init_ncurses();
void end_ncurses();
void reset_screen();
// void display_centered_text(WINDOW *win, int starty, int width, const char *text);

#endif
